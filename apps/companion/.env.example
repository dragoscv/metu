# Companion env example — copy to `.env` for local dev.
VITE_METU_API=http://localhost:3000
VITE_METU_HUB=http://localhost:3001

# Public OAuth client id registered in METU `/apps`.
# Companion is a "public" client (no secret, PKCE).
VITE_METU_COMPANION_CLIENT_ID=metu_app_companion
