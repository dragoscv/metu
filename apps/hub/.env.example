# 24891 by default; override with HUB_PORT
HUB_PORT=24891

# Optional: shared secret to gate /internal/* admin endpoints in dev
HUB_INTERNAL_SECRET=

# Web app base URL — used to forward `tool.result` envelopes back to it.
WEB_INTERNAL_URL=http://localhost:24890
